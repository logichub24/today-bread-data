# 이 파일을 .env로 복사한 뒤 값을 채운다. .env는 커밋되지 않는다.
#
# 매일 도는 자동 수집(매장·행사)에는 인증키가 필요 없다.
# 아래 키는 좌표가 빠진 매장을 손으로 보강할 때만(npm run data:geocode) 쓴다.

# 브이월드 지오코더 2.0 인증키. https://www.vworld.kr → 오픈API → 인증키 발급
# 주의: VITE_ 접두사를 붙이면 안 된다. 앱 번들에 그대로 실린다.
VWORLD_KEY=
